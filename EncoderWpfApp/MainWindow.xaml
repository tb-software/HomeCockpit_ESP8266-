<Window x:Class="EncoderWpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Encoder Input" Icon="HomeCockpit_Icon_DEFAULT.ico" Height="200" Width="300">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_Optionen">
                <MenuItem x:Name="AutostartMenuItem" Header="_Autostart" IsCheckable="True" />
                <MenuItem x:Name="ComPortMenuItem" Header="_COM-Port" />
                <MenuItem x:Name="KeyMappingMenuItem" Header="_Key Mapping" />
                <MenuItem x:Name="SimulatorMenuItem" Header="_Simulator" />
            </MenuItem>
        </Menu>
        <StackPanel Margin="10">
            <TextBlock x:Name="ConnectionText" Text="Disconnected" Margin="0,0,0,5" />
            <TextBlock x:Name="LastMessageText" Text="No data" Margin="0,0,0,5" />
            <TextBlock x:Name="VersionText" Text="Version: ?" Margin="0,0,0,5" />
            <TextBlock x:Name="KeyText" Text="No key" />
        </StackPanel>
    </DockPanel>
</Window>
