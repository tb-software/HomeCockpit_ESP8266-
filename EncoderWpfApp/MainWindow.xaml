<Window x:Class="EncoderWpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Encoder Input" Height="200" Width="300">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_Optionen">
                <MenuItem x:Name="AutostartMenuItem" Header="_Autostart" IsCheckable="True" />
            </MenuItem>
        </Menu>
        <StackPanel Margin="10">
            <TextBlock x:Name="ConnectionText" Text="Disconnected" Margin="0,0,0,5" />
            <TextBlock x:Name="LastMessageText" Text="No data" Margin="0,0,0,5" />
            <TextBlock x:Name="KeyText" Text="No key" />
        </StackPanel>
    </DockPanel>
</Window>
